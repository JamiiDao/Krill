// #[cfg(all(feature = "client", feature = "server", not(debug_assertions)))]
// compile_error!("Features `client` and `server` cannot be enabled at the same time for production deployments when running Rust compiler in release mode.");
